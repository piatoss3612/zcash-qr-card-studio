Edit this developer payment card concept into revision 05. Output ONE flat landscape CARD ONLY, full bleed opaque warm ivory, no surrounding white page, no content outside the card. Preserve the existing approved pixel Vizorcat identity, proportions, face, pose, ears, paws, tail and pixel style. Preserve the small golden Zcash logo upper right.

Main correction: remove the external GitHub link row completely. Move the featured repository INSIDE the ivory card, directly BELOW the two-line introduction, as quiet identity information. Exact repository text: "rowan/privacy-tools" with a small GitHub icon immediately before it. Regular-weight dark grey text, smaller than the bio but still legible at an embed width of 480px. NO underline, NO arrow beside the repository, NO pill, NO container, NO link styling. It is a plain project credit, not a separate clickable item.

Layout: left column upper section contains medium-weight geometric grotesk name "rowan", then exact bio "Building tools for" / "a more private web.", then the single repository credit with a clear but modest gap. Name weight should be much lighter than the extra-bold original, contemporary geometric sans like Space Grotesk Medium; body like Geist Regular. Rebalance vertically so this information block does not crowd the QR. Enlarge the QR visual placeholder to about 25% of total card width and put it lower-left with ample clean ivory quiet space on all sides. Move the bio/name upward or reduce their excessive size as needed to make space, never overlap QR. The unchanged pixel cat is freely placed in the right half, feet near the QR bottom, with generous whitespace and no panel. Retain ONE discreet diagonal arrow at the card bottom-right as the ONLY action cue, smaller and less heavy than in the reference. Whole-card intended action is direct zcash: ZIP-321 wallet launch, NOT an intermediary web page. Do not print URI strings.
No Support text, no scan instructions, no duplicate handle, no external caption, no frames, borders, dividers, shadows, rings, white tiles or scenery. Clear hierarchy and restrained type, visually cohesive as one personal business card. This is a layout concept with a non-production QR placeholder; no disclaimer printed in the design.
